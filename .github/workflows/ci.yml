name: Continuous Integration
on: 
  pull_request:
    types:
      - opened
    branches:
      - 'feature/**'

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v1